export class OutlookCalendarService {
  static async listEvents(token: string, start: string, end: string): Promise<any[]> {
    // Placeholder: call Microsoft Graph API to list events
    return [];
  }

  static async createEvent(token: string, event: any): Promise<any> {
    // Placeholder: call Microsoft Graph API to create event
    return {};
  }
}
