
import { Button } from "@/components/ui/button";
import { CalendarPlus } from "lucide-react";

// Nota: Esta componente foi mantido por compatibilidade, mas não tem mais elementos visíveis
export const CalendarFooter = () => {
  return (
    <div className="p-0 bg-transparent"></div>
  );
};

